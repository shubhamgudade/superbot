# Fixed image assets

The `+kirk` command expects its fixed source image at:

`assets/kirk-source.jpg`

The image is intentionally kept out of the public repository. Put the supplied source image at that path after cloning.
